pipeline {
    agent any
    tools {
        maven 'Maven3'
    }
    stages {
        stage('checkout') {
            steps
            checkout([$class: 'GitSCM', branches: [[name: '*/master']], extensions: [], userRemoteConfigs: [[url: 'https://github.com/Sureshvenkatram/Java-backend-projects.git']]])
            
        }
         stage('Build') {
             steps {
                 sh 'mvn clean install -f Java-backend-projects/pom.xml'
             }
         }
    }
}
